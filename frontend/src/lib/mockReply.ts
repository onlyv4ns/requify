export function mockReply(prompt: string) {
  return `Here's a quick take on "${prompt.trim()}" — this is a demo reply since Nexa isn't wired to a real model yet. Connect an API key to get real answers.`;
}
